name: PR Pipeline

on:
  pull_request:

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      services: ${{ steps.filter.outputs.changes }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            owners:
              - 'src/backend/owners/**'
            pets:
              - 'src/backend/pets/**'
            vets:
              - 'src/backend/vets/**'
            visits:
              - 'src/backend/visits/**'
            notifications:
              - 'src/backend/notifications/**'
            gateway:
              - 'src/backend/gateway/**'

  test:
    needs: detect-changes
    if: needs.detect-changes.outputs.services != '[]'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: ${{ fromJson(needs.detect-changes.outputs.services) }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Restore
        run: dotnet restore src/
      - name: Build ${{ matrix.service }}
        run: dotnet build src/backend/${{ matrix.service }}/ --configuration Release --no-restore
      - name: Test ${{ matrix.service }}
        run: dotnet test src/backend/${{ matrix.service }}/ --configuration Release --no-build --verbosity normal

  sonar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: SonarCloud Analyze
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          dotnet tool install --global dotnet-sonarscanner
          dotnet sonarscanner begin /k:"petclinic-platform" /o:"<org>" /d:sonar.token="$SONAR_TOKEN" /d:sonar.host.url="https://sonarcloud.io"
          dotnet build src/ --configuration Release
          dotnet test src/ --configuration Release --no-build --logger trx --collect:"XPlat Code Coverage"
          dotnet sonarscanner end /d:sonar.token="$SONAR_TOKEN"
